//! Branch registry — your subsystem modules.
//!
//! Empty to start: the tree's built-in pulse lives in `src/pulse.rs`, so
//! everything here is yours. `bonsai branch <name>` adds a `pub mod` line here
//! and a start call in `src/main.rs`. Keep the `// bonsai:mod` marker intact.

// bonsai:mod
